#ifndef UTILS_H
#define UTILS_H
#include <cmath>

inline double aproxNumber(double number) {
  return floor(number + 0.5);
}

inline float aproxNumber(float number) {
  return floor(number + 0.5);
}

#endif // UTILS_H ///:~
